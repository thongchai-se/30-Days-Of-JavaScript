// ============================================
// วัน 20 — ไฟล์ฝึก (ไม่มีเฉลย)
// เน้นเขียนให้สะอาด ไม่ใช่โจทย์คำนวณ
// ============================================

// ข้อ 1: โค้ดด้านล่างอ่านยาก — เขียนใหม่ให้ชื่อสื่อความหมาย
// (อย่าเพิ่งลบของเดิม เขียนเวอร์ชันใหม่ข้างใต้)
let a = 25
let b = 'Tong'
let c = true
function f(x, y) {
  return x + ' is ' + y
}
// เขียนเวอร์ชันสะอาดตรงนี้


// ข้อ 2: แยกงานในฟังก์ชันนี้ให้ชัด (validate / format / print)
function doStuff(n) {
  if (n == null || n === '') return
  let t = String(n).trim().toUpperCase()
  console.log('RESULT:' + t)
  return t
}
// เขียนตรงนี้


// ข้อ 3: สร้างค่าคงที่และฟังก์ชันตาม convention
// - ค่าคงที่ตัวพิมพ์ใหญ่
// - ฟังก์ชัน camelCase
// - คลาส (ถ้ามี) PascalCase
// เขียนตรงนี้


// ข้อ 4: เขียน comment สั้นๆ ว่า style guide ช่วยอะไรคุณบ้าง
